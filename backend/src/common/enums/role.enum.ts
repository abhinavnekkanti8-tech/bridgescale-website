/**
 * MembershipRole enum — mirrors the Prisma enum for use in TypeScript decorators and guards.
 */
export enum MembershipRole {
  STARTUP_ADMIN = 'STARTUP_ADMIN',
  STARTUP_MEMBER = 'STARTUP_MEMBER',
  OPERATOR = 'OPERATOR',
  PLATFORM_ADMIN = 'PLATFORM_ADMIN',
  DEAL_DESK = 'DEAL_DESK',
}
